DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
JWT_SECRET=replace-with-long-random-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-strong-password
NODE_ENV=production
# 正式環境預設不會在冷啟動時執行 DDL；請維持未設定。
# 只有明確要初始化 legacy 本機資料庫時才設為 1；此流程不是完整正式 schema migration。
# RUN_DB_DDL_ON_BOOT=1
# 選填：Postgres 連線池上限（正式環境預設 2）
# PG_POOL_MAX=2
ADMIN_LOGIN_PATH=/admin-portal-change-this-to-random-path
# 選填：管理員登入防暴力破解（預設 15 分鐘內同 IP 失敗 8 次即暫停；POST 另有限流 10 次／15 分鐘）
# ADMIN_LOGIN_THROTTLE_WINDOW_MIN=15
# ADMIN_LOGIN_MAX_ATTEMPTS=8
# ADMIN_LOGIN_POST_RATE_MAX=10
LIFF_ID=your-liff-id
# 通用活動平台共用 LIFF；輪盤可再用 WHEEL_LIFF_ID 覆蓋。
# GAMES_LIFF_ID=your-games-liff-id
# WHEEL_LIFF_ID=your-wheel-liff-id
# 正式環境建議維持 1，要求後端驗證 LIFF access token。
# LIFF_TOKEN_ENFORCE=1
# 金豬食堂獨立 LIFF（請勿覆蓋上面的既有 LIFF_ID）
GOLD_PIG_LIFF_ID=your-gold-pig-liff-id
# 付款服務建立正式訂位時使用；只放在伺服器端，不可出現在前端程式
GOLD_PIG_BOOKING_API_KEY=replace-with-a-long-random-server-key
# 活動頁仍在功能展示階段時才開啟；正式收款上線前改為 0
GOLD_PIG_DEMO_MODE=0
# 可呼叫示範訂位 API 的活動頁來源，多個網址以逗號分隔
GOLD_PIG_ALLOWED_ORIGINS=https://twopenrice-ops.github.io,https://tw.openrice.com
# LIFF Endpoint URL 若為 https://你的網域/liff（常見），勿設下列變數。永久連結會用 /lottery、/邀請碼，避免變成 /liff/liff/...
# 若 Endpoint 設為網站根目錄 https://你的網域（不含 /liff），請設為 1，永久連結才會帶 /liff/ 前綴。
# LIFF_ENDPOINT_IS_SITE_ROOT=1
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
# 第二個 LINE 官方帳號（獨立 webhook 與聯絡人資料）。未使用可留空。
# LINE2_CHANNEL_SECRET=your-second-oa-channel-secret
# LINE2_CHANNEL_ACCESS_TOKEN=your-second-oa-channel-access-token
# LINE2_OA_KEY=second-oa
# 必須為完整 https 網址（例如 https://line.me/R/ti/p/@xxx）。勿填相對路徑如 liff/xxx，否則在邀請頁會變成 /liff/liff/xxx 而錯誤。
LINE_OFFICIAL_ADD_FRIEND_URL=https://line.me/R/ti/p/@your-line-official
# 選填：LIFF「兌換方式」區塊文案，可用 \\n 換行；留空則使用預設三步驟說明
# LIFF_REDEMPTION_NOTE=第一行\\n第二行
# 選填：LIFF 內「官方活動／完整辦法」連結（預設為 OpenRice 春日活動短網址）
# LIFF_CAMPAIGN_PAGE_URL=https://tinyurl.com/mv3f9wfv
# 好友加碼：程式內每人最多僅發 1 次加碼刮次；需累計 LIFF_INVITE_FRIENDS_PER_DRAW 位好友完成任務才發放。設 0 可關閉加碼。
LIFF_INVITE_BONUS_MAX=1
# 每幾位好友完成加好友任務，邀請人獲 1 次加碼刮次（預設 2）
# LIFF_INVITE_FRIENDS_PER_DRAW=2
# 選填：LINE 抓圖用 HTTPS 網域（勿結尾斜線）。會與 URL、PUBLIC_SITE_URL 等合併並自動探測可連線的圖片網址。
# 若與主站不同（例如 CDN），可只設此變數。
# LINE_PUSH_IMAGE_BASE_URL=https://你的圖片或主站網域
# LINE_PUSH_PUBLIC_BASE_URL=https://你的網域
# PUBLIC_SITE_URL=https://你的網域
#
# 後台「邀請提醒」上傳的推播圖片會以 https://你的網域/p/line-media/{uuid} 提供給 LINE 抓取，
# 與靜態 /images 相同需為公開 HTTPS；請務必設定上述其中一個正式網域變數（Netlify 通常已有 URL）。

# ===========================
# Email 推播 (Brevo / Sendinblue)
# ===========================
# 必填：Brevo API key (xkeysib-...)，從 Brevo Settings → SMTP & API → API Keys 取得
# BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 必填：預設寄件人 email（必須在 Brevo 驗證過）
# BREVO_SENDER_EMAIL=noreply@yourdomain.com
# 選填：預設寄件人顯示名稱
# BREVO_SENDER_NAME=OpenRice 開飯喇
# 選填：Brevo webhook 驗證 secret（要求 /webhooks/brevo?s=xxx 或 header x-brevo-secret 匹配）
# BREVO_WEBHOOK_SECRET=random-long-secret

# ===========================
# Email 推播 (SureNotify)
# ===========================
# EMAIL_PROVIDER=surenotify
# SURENOTIFY_API_KEY=replace-with-api-key
# SURENOTIFY_SENDER_EMAIL=noreply@yourdomain.com
# SURENOTIFY_SENDER_NAME=OpenRice 開飯喇
# Webhook 驗證是否嚴格執行；正式環境建議為 1。
# SURENOTIFY_WEBHOOK_STRICT=1

# ===========================
# 訂位熱榜自動推播（Booking Leaderboard）
# ===========================
# 必填：openrice-booking-report 的 Supabase 連線字串（transaction pooler, 6543）。
# 從 booking-report Supabase 後台 → Settings → Database → Connection string → Transaction pooler 複製，密碼換成真的。
# BOOKING_REPORT_DATABASE_URL=postgresql://postgres.agxpbjgatcxjlwdkrqev:PASSWORD@aws-x-ap-southeast-1.pooler.supabase.com:6543/postgres
# 選填：榜單取前幾名（預設 5）
# LEADERBOARD_TOP_N=5
# 必須有：SCHEDULED_RUNNER_SECRET（既有變數，5 分鐘 runner 與每月觸發共用；沒設則排程不會送出）
# SCHEDULED_RUNNER_SECRET=replace-with-long-random-secret
