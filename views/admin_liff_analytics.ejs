<%-
  include('layout', {
    user: user,
    isAdmin: isAdmin,
    bodyClass: bodyClass,
    body: `
    <section class="card liff-hero">
      <div class="liff-hero-inner">
        <h1>今天吃什麼 — 用戶行為分析</h1>
        <p class="muted" style="margin-top:6px;">
          Random Rice LIFF 的即時概覽、核心轉換漏斗與每日趨勢。資料源同步自 Supabase user_events 表。
        </p>
        <div class="liff-hero-meta">
          <span class="liff-meta-pill">即時更新</span>
          <span class="liff-meta-pill liff-meta-pill--muted" id="liff-last-updated">載入中…</span>
          <button type="button" class="liff-refresh-btn" id="btn-liff-refresh">重新整理</button>
        </div>
      </div>
    </section>

    <section class="card">
      <h2>今日即時概覽</h2>
      <div class="muted" style="font-size:12px;margin-bottom:12px;">以台灣時區 00:00 為起點計算。</div>
      <div class="liff-kpi-grid">
        <div class="liff-kpi-card">
          <div class="liff-kpi-title">打開 LIFF</div>
          <div class="liff-kpi-split">
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">次</div>
              <div class="liff-kpi-value" id="kpi-open-sessions">—</div>
            </div>
            <div class="liff-kpi-divider"></div>
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">人</div>
              <div class="liff-kpi-value" id="kpi-open-users">—</div>
            </div>
          </div>
          <div class="liff-kpi-hint">次 = distinct session_id；人 = distinct line_id</div>
        </div>

        <div class="liff-kpi-card">
          <div class="liff-kpi-title">抽選</div>
          <div class="liff-kpi-split">
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">次</div>
              <div class="liff-kpi-value" id="kpi-draw-events">—</div>
            </div>
            <div class="liff-kpi-divider"></div>
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">人</div>
              <div class="liff-kpi-value" id="kpi-draw-users">—</div>
            </div>
          </div>
          <div class="liff-kpi-hint">submit_draw + redraw</div>
        </div>

        <div class="liff-kpi-card">
          <div class="liff-kpi-title">點訂位</div>
          <div class="liff-kpi-split">
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">次</div>
              <div class="liff-kpi-value" id="kpi-click-events">—</div>
            </div>
            <div class="liff-kpi-divider"></div>
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">人</div>
              <div class="liff-kpi-value" id="kpi-click-users">—</div>
            </div>
          </div>
          <div class="liff-kpi-hint">restaurant_click（核心轉換）</div>
        </div>

        <div class="liff-kpi-card liff-kpi-card--highlight">
          <div class="liff-kpi-title">抽 → 點訂位 轉換率</div>
          <div class="liff-kpi-split">
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">次</div>
              <div class="liff-kpi-value" id="kpi-conv-event">—</div>
            </div>
            <div class="liff-kpi-divider"></div>
            <div class="liff-kpi-half">
              <div class="liff-kpi-sublabel">人</div>
              <div class="liff-kpi-value" id="kpi-conv-user">—</div>
            </div>
          </div>
          <div class="liff-kpi-hint">次 = clicks/shown 事件數；人 = 有點過的人/有看過結果的人</div>
        </div>
      </div>
    </section>

    <section class="card">
      <div class="liff-section-head">
        <h2>玩的人裡面，有幾個是你的會員</h2>
        <div class="liff-range-toggle">
          <label><input type="radio" name="aud-days" value="7" checked /> 近 7 天</label>
          <label><input type="radio" name="aud-days" value="30" /> 近 30 天</label>
          <label><input type="radio" name="aud-days" value="3650" /> 全部</label>
        </div>
      </div>
      <p class="liff-aud-lead" id="aud-lead">載入中…</p>
      <div class="liff-aud-grid" id="aud-grid" hidden>
        <div class="liff-aud-item">
          <div class="liff-aud-k">你現在的 LINE 好友</div>
          <div class="liff-aud-v" id="aud-member-people">—</div>
          <div class="liff-aud-sub" id="aud-member-events">—</div>
          <div class="liff-aud-note">加了現在這個官方帳號、你發訊息找得到的人。</div>
        </div>
        <div class="liff-aud-item">
          <div class="liff-aud-k">舊官方帳號時期的人</div>
          <div class="liff-aud-v" id="aud-old-people">—</div>
          <div class="liff-aud-sub" id="aud-old-events">—</div>
          <div class="liff-aud-note">看得出是誰，但他加的是換掉的那個舊帳號，現在發訊息找不到他。</div>
        </div>
        <div class="liff-aud-item">
          <div class="liff-aud-k">還不認識的人</div>
          <div class="liff-aud-v" id="aud-unknown-people">—</div>
          <div class="liff-aud-sub" id="aud-unknown-events">—</div>
          <div class="liff-aud-note">有玩過，但對不到你名單裡的任何一個人。</div>
        </div>
      </div>
      <p class="liff-aud-foot" id="aud-foot" hidden></p>
      <div class="liff-aud-actions" id="aud-actions" hidden></div>
      <div class="table-wrap" id="aud-table-wrap" hidden>
        <table class="liff-users-table liff-aud-table">
          <thead>
            <tr>
              <th>會員</th>
              <th style="text-align:right;">開啟過活動頁</th>
              <th style="text-align:right;">擲過骰子</th>
              <th style="text-align:right;">點過餐廳</th>
              <th style="text-align:right;">點過訂位</th>
              <th>最後一次來</th>
            </tr>
          </thead>
          <tbody id="aud-tbody"></tbody>
        </table>
      </div>
    </section>

    <section class="card">
      <div class="liff-section-head">
        <h2>核心轉換漏斗</h2>
        <div class="liff-range-toggle">
          <label><input type="radio" name="funnel-metric" value="users" checked /> 以人數</label>
          <label><input type="radio" name="funnel-metric" value="events" /> 以次數</label>
        </div>
        <div class="liff-range-toggle">
          <label><input type="radio" name="funnel-days" value="7" checked /> 7 天</label>
          <label><input type="radio" name="funnel-days" value="30" /> 30 天</label>
        </div>
      </div>
      <div class="muted" style="font-size:12px;margin-bottom:12px;">打開 LIFF → 首次抽選 → 看到結果 → 點訂位。「人數」= distinct line_id；「次數」= 事件總數。表格同時顯示兩者方便比對。</div>
      <div class="liff-funnel-wrap">
        <canvas id="funnel-chart"></canvas>
      </div>
      <table class="liff-funnel-table">
        <thead>
          <tr>
            <th rowspan="2">步驟</th>
            <th colspan="3" class="liff-col-group">人數</th>
            <th colspan="3" class="liff-col-group">次數</th>
          </tr>
          <tr>
            <th style="text-align:right;">人</th>
            <th style="text-align:right;">相對上一步</th>
            <th style="text-align:right;">相對首步</th>
            <th style="text-align:right;">次</th>
            <th style="text-align:right;">相對上一步</th>
            <th style="text-align:right;">相對首步</th>
          </tr>
        </thead>
        <tbody id="funnel-tbody">
          <tr><td colspan="7" class="muted" style="text-align:center;padding:18px 0;">載入中…</td></tr>
        </tbody>
      </table>
    </section>

    <section class="card">
      <div class="liff-section-head">
        <h2>玩過的用戶</h2>
        <div class="liff-range-toggle">
          <label><input type="radio" name="users-days" value="1" /> 今日</label>
          <label><input type="radio" name="users-days" value="7" checked /> 7 天</label>
          <label><input type="radio" name="users-days" value="30" /> 30 天</label>
        </div>
      </div>
      <div class="muted" style="font-size:12px;margin-bottom:12px;">
        顯示有抽過或打開過 LIFF 的真實用戶。「已在 CRM」表示這位有過 LINE login 紀錄（可對到顯示名稱）；「未在 CRM」可能是新訪客或測試帳號。點 line_id 可複製。
      </div>
      <div class="table-wrap">
        <table class="liff-users-table">
          <thead>
            <tr>
              <th>用戶</th>
              <th style="text-align:right;">Session</th>
              <th style="text-align:right;">抽次</th>
              <th style="text-align:right;">點訂位</th>
              <th style="text-align:right;">轉換率</th>
              <th>最後活動</th>
            </tr>
          </thead>
          <tbody id="users-tbody">
            <tr><td colspan="6" class="muted" style="text-align:center;padding:18px 0;">載入中…</td></tr>
          </tbody>
        </table>
      </div>
    </section>

    <section class="card">
      <div class="liff-section-head">
        <h2>每日趨勢</h2>
        <div class="liff-range-toggle">
          <label><input type="radio" name="trend-days" value="7" /> 7 天</label>
          <label><input type="radio" name="trend-days" value="30" checked /> 30 天</label>
        </div>
      </div>
      <div class="muted" style="font-size:12px;margin-bottom:12px;">DAU 與點訂位以左側 Y 軸；轉換率（%）以右側 Y 軸。</div>
      <div class="liff-trend-wrap">
        <canvas id="trend-chart"></canvas>
      </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.js"></script>
    <script>
    (function () {
      var $ = function (id) { return document.getElementById(id); };

      var funnelChart = null;
      var trendChart = null;

      function fmt(n) {
        if (n == null) return '—';
        return Number(n).toLocaleString('zh-Hant-TW');
      }
      function fmtPct(p) {
        if (p == null) return '—';
        return Number(p).toFixed(2) + '%';
      }
      function nowLabel() {
        var d = new Date();
        var hh = String(d.getHours()).padStart(2, '0');
        var mm = String(d.getMinutes()).padStart(2, '0');
        return d.getFullYear() + '-' +
          String(d.getMonth() + 1).padStart(2, '0') + '-' +
          String(d.getDate()).padStart(2, '0') + ' ' +
          hh + ':' + mm;
      }

      // ---------------- 今日即時概覽 ----------------
      var overviewIds = [
        'kpi-open-sessions', 'kpi-open-users',
        'kpi-draw-events',   'kpi-draw-users',
        'kpi-click-events',  'kpi-click-users',
        'kpi-conv-event',    'kpi-conv-user'
      ];
      function loadOverview() {
        return fetch('/admin/liff/random-rice/api/overview')
          .then(function (r) { return r.json(); })
          .then(function (resp) {
            if (!resp.ok) throw new Error(resp.detail || 'overview failed');
            var d = resp.data || {};
            var open = d.open || {}, draw = d.draw || {}, click = d.click || {}, conv = d.conv || {};
            $('kpi-open-sessions').textContent = fmt(open.sessions);
            $('kpi-open-users').textContent    = fmt(open.users);
            $('kpi-draw-events').textContent   = fmt(draw.events);
            $('kpi-draw-users').textContent    = fmt(draw.users);
            $('kpi-click-events').textContent  = fmt(click.events);
            $('kpi-click-users').textContent   = fmt(click.users);
            $('kpi-conv-event').textContent    = fmtPct(conv.by_event_pct);
            $('kpi-conv-user').textContent     = fmtPct(conv.by_user_pct);
          })
          .catch(function (e) {
            console.error('overview load failed:', e);
            overviewIds.forEach(function (id) { $(id).textContent = '錯誤'; });
          });
      }

      // ---------------- 核心漏斗 ----------------
      var lastFunnelSteps = [];
      function loadFunnel(days) {
        return fetch('/admin/liff/random-rice/api/funnel?days=' + encodeURIComponent(days))
          .then(function (r) { return r.json(); })
          .then(function (resp) {
            if (!resp.ok) throw new Error(resp.detail || 'funnel failed');
            lastFunnelSteps = resp.data || [];
            renderFunnel();
          })
          .catch(function (e) {
            console.error('funnel load failed:', e);
            $('funnel-tbody').innerHTML = '<tr><td colspan="7" class="muted" style="text-align:center;padding:18px 0;">載入失敗</td></tr>';
          });
      }

      function currentFunnelMetric() {
        return (document.querySelector('input[name="funnel-metric"]:checked') || {}).value || 'users';
      }

      function renderFunnel() {
        renderFunnelChart(lastFunnelSteps, currentFunnelMetric());
        renderFunnelTable(lastFunnelSteps);
      }

      function renderFunnelChart(steps, metric) {
        var isUsers = metric === 'users';
        var labels = steps.map(function (s) { return s.label; });
        var counts = steps.map(function (s) { return isUsers ? s.users : s.events; });
        var datasetLabel = isUsers ? '人數（distinct line_id）' : '事件次數';
        var ctx = $('funnel-chart').getContext('2d');
        if (funnelChart) funnelChart.destroy();
        funnelChart = new Chart(ctx, {
          type: 'bar',
          data: {
            labels: labels,
            datasets: [{
              label: datasetLabel,
              data: counts,
              backgroundColor: isUsers
                ? ['#bfdbfe','#93c5fd','#60a5fa','#3b82f6']
                : ['#fde68a','#fcd34d','#fbbf24','#FCC726'],
              borderColor: isUsers ? '#2563eb' : '#facc15',
              borderWidth: 1,
              borderRadius: 6
            }]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
              legend: { display: true, position: 'bottom' },
              tooltip: {
                callbacks: {
                  afterLabel: function (ctx) {
                    var step = steps[ctx.dataIndex];
                    if (!step) return '';
                    var fromPrev = isUsers ? step.users_from_prev_pct : step.events_from_prev_pct;
                    var fromFirst = isUsers ? step.users_from_first_pct : step.events_from_first_pct;
                    var lines = [];
                    if (fromPrev != null) lines.push('相對上一步: ' + fromPrev.toFixed(2) + '%');
                    if (fromFirst != null) lines.push('相對首步: ' + fromFirst.toFixed(2) + '%');
                    return lines;
                  }
                }
              }
            },
            scales: {
              x: { grid: { display: false } },
              y: { beginAtZero: true, ticks: { precision: 0 } }
            }
          }
        });
      }

      function renderFunnelTable(steps) {
        var tbody = $('funnel-tbody');
        if (!steps.length) {
          tbody.innerHTML = '<tr><td colspan="7" class="muted" style="text-align:center;padding:18px 0;">區間內沒有資料</td></tr>';
          return;
        }
        tbody.innerHTML = steps.map(function (s) {
          var uPrev = (s.users_from_prev_pct == null) ? '—' : fmtPct(s.users_from_prev_pct);
          var uFirst = (s.users_from_first_pct == null) ? '—' : fmtPct(s.users_from_first_pct);
          var ePrev = (s.events_from_prev_pct == null) ? '—' : fmtPct(s.events_from_prev_pct);
          var eFirst = (s.events_from_first_pct == null) ? '—' : fmtPct(s.events_from_first_pct);
          return '<tr>' +
            '<td>' + s.label + '<span class="liff-step-key">' + s.key + '</span></td>' +
            '<td style="text-align:right;font-weight:600;color:#2563eb;">' + fmt(s.users) + '</td>' +
            '<td style="text-align:right;">' + uPrev + '</td>' +
            '<td style="text-align:right;">' + uFirst + '</td>' +
            '<td style="text-align:right;font-weight:600;color:#b45309;">' + fmt(s.events) + '</td>' +
            '<td style="text-align:right;">' + ePrev + '</td>' +
            '<td style="text-align:right;">' + eFirst + '</td>' +
            '</tr>';
        }).join('');
      }

      // ---------------- 會員視角：玩的人裡面有幾個是我的會員 ----------------
      function rangeLabel(days) {
        var n = Number(days);
        if (n === 7) return '近 7 天';
        if (n === 30) return '近 30 天';
        return '目前為止';
      }

      function loadAudience(days) {
        return fetch('/admin/liff/random-rice/api/audience?days=' + encodeURIComponent(days))
          .then(function (r) { return r.json(); })
          .then(function (resp) {
            if (!resp.ok) throw new Error(resp.detail || 'audience failed');
            renderAudience(resp.data || {}, days);
          })
          .catch(function (e) {
            console.error('audience load failed:', e);
            hideAudienceDetail();
            $('aud-lead').textContent = '這段數字讀不到，按右上角的重新整理再試一次。';
          });
      }

      function hideAudienceDetail() {
        $('aud-grid').hidden = true;
        $('aud-foot').hidden = true;
        $('aud-actions').hidden = true;
        $('aud-table-wrap').hidden = true;
      }

      function renderAudience(d, days) {
        var member = d.member || {}, old = d.old || {}, unknown = d.unknown || {}, anon = d.anon || {};
        var label = rangeLabel(days);
        var people = Number(d.people_with_id || 0);
        var memberPeople = Number(member.people || 0);
        var anonEvents = Number(anon.events || 0);

        // 完全沒資料：講一句話，不要留一排 0 讓人以為壞掉
        if (!people && !anonEvents) {
          hideAudienceDetail();
          $('aud-lead').textContent = label + '沒有人打開過活動頁，所以還沒有數字可以看。';
          return;
        }

        var lead = label + '有 <b>' + fmt(people) + '</b> 個人用過活動頁，' +
                   '其中 <b>' + fmt(memberPeople) + '</b> 位是你現在的 LINE 好友。';
        if (people > 0 && memberPeople === 0) {
          lead += ' 也就是說，現在玩的人你都還發不到訊息。';
        }
        $('aud-lead').innerHTML = lead;

        $('aud-grid').hidden = false;
        $('aud-member-people').textContent = fmt(memberPeople) + ' 人';
        $('aud-member-events').textContent = '共 ' + fmt(Number(member.events || 0)) + ' 次使用';
        $('aud-old-people').textContent = fmt(Number(old.people || 0)) + ' 人';
        $('aud-old-events').textContent = '共 ' + fmt(Number(old.events || 0)) + ' 次使用';
        $('aud-unknown-people').textContent = fmt(Number(unknown.people || 0)) + ' 人';
        $('aud-unknown-events').textContent = '共 ' + fmt(Number(unknown.events || 0)) + ' 次使用';

        var notes = [];
        if (anonEvents > 0) {
          notes.push('另外有 ' + fmt(anonEvents) + ' 次使用沒有留下身分（不是從 LINE 裡面打開的），這些算不出是誰。');
        }
        if (memberPeople > 0) {
          notes.push('會員的部分你可以直接發訊息追，其他人要先想辦法讓他加好友。');
        }
        if (notes.length) {
          $('aud-foot').hidden = false;
          $('aud-foot').textContent = notes.join(' ');
        } else {
          $('aud-foot').hidden = true;
        }

        renderAudienceActions(d.member_actions || {}, memberPeople);
        renderAudienceMembers(d.members || []);
      }

      function renderAudienceActions(acts, memberPeople) {
        var box = $('aud-actions');
        if (!memberPeople) { box.hidden = true; box.innerHTML = ''; return; }
        var items = [
          ['開啟過活動頁', acts.open],
          ['擲過骰子選餐廳', acts.draw],
          ['點過餐廳', acts.pin],
          ['點過訂位', acts.booking]
        ];
        box.innerHTML = items.map(function (it) {
          return '<span class="liff-aud-act"><b>' + fmt(Number(it[1] || 0)) + '</b> 位會員' + it[0] + '</span>';
        }).join('');
        box.hidden = false;
      }

      function renderAudienceMembers(rows) {
        var wrap = $('aud-table-wrap');
        var tbody = $('aud-tbody');
        if (!rows.length) { wrap.hidden = true; tbody.innerHTML = ''; return; }
        tbody.innerHTML = rows.map(function (m) {
          var name = m.display_name
            ? escapeHtml(m.display_name)
            : '<span class="liff-user-anon">還沒抓到名字</span>';
          var avatarHtml = m.picture_url
            ? '<img src="' + escapeHtml(m.picture_url) + '" alt="" class="liff-user-avatar" />'
            : '<div class="liff-user-avatar liff-user-avatar--blank">' + escapeHtml((m.display_name || '?').slice(0, 1)) + '</div>';
          return '<tr>' +
            '<td class="liff-user-cell">' +
              '<div class="liff-user-row">' +
                avatarHtml +
                '<div class="liff-user-meta">' +
                  '<div class="liff-user-name">' + name + '</div>' +
                  '<div class="liff-aud-since">共來過 ' + fmt(m.sessions) + ' 次</div>' +
                '</div>' +
              '</div>' +
            '</td>' +
            '<td style="text-align:right;">' + fmt(m.opens) + '</td>' +
            '<td style="text-align:right;">' + fmt(m.draws) + '</td>' +
            '<td style="text-align:right;">' + fmt(m.pins) + '</td>' +
            '<td style="text-align:right;font-weight:600;">' + fmt(m.bookings) + '</td>' +
            '<td>' + fmtTime(m.last_seen) + '</td>' +
          '</tr>';
        }).join('');
        wrap.hidden = false;
      }

      // ---------------- 玩過的用戶清單 ----------------
      function loadUsers(days) {
        return fetch('/admin/liff/random-rice/api/users?days=' + encodeURIComponent(days))
          .then(function (r) { return r.json(); })
          .then(function (resp) {
            if (!resp.ok) throw new Error(resp.detail || 'users failed');
            renderUsersTable(resp.data || []);
          })
          .catch(function (e) {
            console.error('users load failed:', e);
            $('users-tbody').innerHTML = '<tr><td colspan="6" class="muted" style="text-align:center;padding:18px 0;">載入失敗</td></tr>';
          });
      }
      function escapeHtml(s) {
        return String(s == null ? '' : s)
          .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
          .replace(/"/g, '&quot;').replace(/'/g, '&#039;');
      }
      function fmtTime(ts) {
        if (!ts) return '—';
        var d = new Date(ts);
        if (isNaN(d.getTime())) return String(ts);
        var pad = function (n) { return String(n).padStart(2, '0'); };
        return d.getFullYear() + '-' + pad(d.getMonth() + 1) + '-' + pad(d.getDate()) +
               ' ' + pad(d.getHours()) + ':' + pad(d.getMinutes());
      }
      function shortUid(uid) {
        if (!uid) return '';
        return uid.slice(0, 6) + '…' + uid.slice(-4);
      }
      function renderUsersTable(rows) {
        var tbody = $('users-tbody');
        if (!rows.length) {
          tbody.innerHTML = '<tr><td colspan="6" class="muted" style="text-align:center;padding:18px 0;">區間內沒有用戶</td></tr>';
          return;
        }
        tbody.innerHTML = rows.map(function (u) {
          var displayName = u.display_name || '<span class="liff-user-anon">未在 CRM 用戶表</span>';
          var convPct = u.shown > 0 ? (Math.round(u.clicks / u.shown * 10000) / 100).toFixed(2) + '%' : '—';
          var avatarHtml = u.picture_url
            ? '<img src="' + escapeHtml(u.picture_url) + '" alt="" class="liff-user-avatar" />'
            : '<div class="liff-user-avatar liff-user-avatar--blank">' + escapeHtml((u.display_name || '?').slice(0, 1)) + '</div>';
          var crmBadge = u.in_crm
            ? '<span class="liff-user-badge liff-user-badge--in">已在 CRM</span>'
            : '<span class="liff-user-badge liff-user-badge--out">未在 CRM</span>';
          return '<tr>' +
            '<td class="liff-user-cell">' +
              '<div class="liff-user-row">' +
                avatarHtml +
                '<div class="liff-user-meta">' +
                  '<div class="liff-user-name">' + displayName + ' ' + crmBadge + '</div>' +
                  '<button type="button" class="liff-user-uid" data-uid="' + escapeHtml(u.line_id) + '" title="點擊複製完整 line_id">' +
                    escapeHtml(shortUid(u.line_id)) +
                  '</button>' +
                '</div>' +
              '</div>' +
            '</td>' +
            '<td style="text-align:right;">' + fmt(u.sessions) + '</td>' +
            '<td style="text-align:right;">' + fmt(u.draws) + '</td>' +
            '<td style="text-align:right;font-weight:600;">' + fmt(u.clicks) + '</td>' +
            '<td style="text-align:right;">' + convPct + '</td>' +
            '<td>' + fmtTime(u.last_seen) + '</td>' +
          '</tr>';
        }).join('');
        // wire up uid copy
        tbody.querySelectorAll('.liff-user-uid').forEach(function (btn) {
          btn.addEventListener('click', function () {
            var uid = btn.getAttribute('data-uid');
            if (!uid) return;
            navigator.clipboard.writeText(uid).then(function () {
              var orig = btn.textContent;
              btn.textContent = '已複製';
              btn.classList.add('copied');
              setTimeout(function () {
                btn.textContent = orig;
                btn.classList.remove('copied');
              }, 1200);
            }).catch(function () { alert('line_id: ' + uid); });
          });
        });
      }

      // ---------------- 每日趨勢 ----------------
      function loadTrend(days) {
        return fetch('/admin/liff/random-rice/api/trend?days=' + encodeURIComponent(days))
          .then(function (r) { return r.json(); })
          .then(function (resp) {
            if (!resp.ok) throw new Error(resp.detail || 'trend failed');
            renderTrendChart(resp.data || []);
          })
          .catch(function (e) {
            console.error('trend load failed:', e);
          });
      }

      function renderTrendChart(rows) {
        var labels = rows.map(function (r) { return r.day; });
        var dau = rows.map(function (r) { return r.dau; });
        var clicks = rows.map(function (r) { return r.clicks; });
        var conv = rows.map(function (r) { return r.conversion_rate_pct; });
        var ctx = $('trend-chart').getContext('2d');
        if (trendChart) trendChart.destroy();
        trendChart = new Chart(ctx, {
          data: {
            labels: labels,
            datasets: [
              {
                type: 'line', label: 'DAU', data: dau,
                borderColor: '#1f2937', backgroundColor: 'rgba(31,41,55,0.08)',
                fill: true, tension: 0.3, yAxisID: 'y'
              },
              {
                type: 'line', label: '點訂位', data: clicks,
                borderColor: '#facc15', backgroundColor: 'rgba(252,204,38,0.12)',
                fill: true, tension: 0.3, yAxisID: 'y'
              },
              {
                type: 'line', label: '轉換率 %', data: conv,
                borderColor: '#dc2626', borderDash: [4, 4],
                backgroundColor: 'transparent', fill: false,
                tension: 0.3, yAxisID: 'y1', pointRadius: 2
              }
            ]
          },
          options: {
            responsive: true,
            maintainAspectRatio: false,
            interaction: { mode: 'index', intersect: false },
            stacked: false,
            plugins: { legend: { position: 'bottom' } },
            scales: {
              x: { grid: { display: false } },
              y:  { type: 'linear', position: 'left',  beginAtZero: true, ticks: { precision: 0 } },
              y1: {
                type: 'linear', position: 'right',
                beginAtZero: true,
                grid: { drawOnChartArea: false },
                ticks: {
                  callback: function (v) { return v + '%'; }
                }
              }
            }
          }
        });
      }

      // ---------------- 觸發 + 互動 ----------------
      function refreshAll() {
        $('liff-last-updated').textContent = '載入中…';
        var funnelDays = (document.querySelector('input[name="funnel-days"]:checked') || {}).value || 7;
        var trendDays = (document.querySelector('input[name="trend-days"]:checked') || {}).value || 30;
        var usersDays = (document.querySelector('input[name="users-days"]:checked') || {}).value || 7;
        var audDays = (document.querySelector('input[name="aud-days"]:checked') || {}).value || 7;
        Promise.all([
          loadOverview(),
          loadAudience(audDays),
          loadFunnel(funnelDays),
          loadUsers(usersDays),
          loadTrend(trendDays)
        ]).then(function () {
          $('liff-last-updated').textContent = '最後更新: ' + nowLabel();
        });
      }

      document.querySelectorAll('input[name="funnel-days"]').forEach(function (el) {
        el.addEventListener('change', function () { loadFunnel(el.value); });
      });
      document.querySelectorAll('input[name="funnel-metric"]').forEach(function (el) {
        el.addEventListener('change', function () { renderFunnel(); });
      });
      document.querySelectorAll('input[name="trend-days"]').forEach(function (el) {
        el.addEventListener('change', function () { loadTrend(el.value); });
      });
      document.querySelectorAll('input[name="users-days"]').forEach(function (el) {
        el.addEventListener('change', function () { loadUsers(el.value); });
      });
      document.querySelectorAll('input[name="aud-days"]').forEach(function (el) {
        el.addEventListener('change', function () { loadAudience(el.value); });
      });
      $('btn-liff-refresh').addEventListener('click', refreshAll);

      refreshAll();
    })();
    </script>

    <style>
      .liff-hero {
        background: linear-gradient(135deg, #fffbeb 0%, #ffffff 100%);
        border: 1px solid #fde68a;
      }
      .liff-hero-meta {
        margin-top: 12px;
        display: flex;
        align-items: center;
        gap: 8px;
        flex-wrap: wrap;
      }
      .liff-meta-pill {
        display: inline-block;
        padding: 4px 10px;
        font-size: 12px;
        font-weight: 600;
        border-radius: 9999px;
        background: #FCC726;
        color: #1f2937;
      }
      .liff-meta-pill--muted { background: #e5e7eb; color: #4b5563; }
      .liff-refresh-btn {
        margin-left: auto;
        padding: 6px 12px;
        font-size: 12px;
        border: 1px solid #e5e7eb;
        background: #fff;
        border-radius: 6px;
        cursor: pointer;
        color: #374151;
      }
      .liff-refresh-btn:hover { background: #f9fafb; border-color: #d1d5db; }

      .liff-kpi-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
        gap: 12px;
      }
      .liff-kpi-card {
        padding: 14px 16px;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        background: #fff;
      }
      .liff-kpi-card--highlight {
        background: #fffbeb;
        border-color: #FCC726;
      }
      .liff-kpi-title {
        font-size: 13px;
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 8px;
      }
      .liff-kpi-split {
        display: flex;
        align-items: stretch;
        gap: 12px;
      }
      .liff-kpi-half { flex: 1; min-width: 0; }
      .liff-kpi-divider {
        width: 1px;
        background: #e5e7eb;
        align-self: stretch;
      }
      .liff-kpi-sublabel {
        font-size: 11px;
        color: #6b7280;
        margin-bottom: 2px;
      }
      .liff-kpi-value {
        font-size: 24px;
        font-weight: 700;
        color: #1f2937;
        line-height: 1.1;
      }
      .liff-kpi-hint { font-size: 11px; color: #9ca3af; margin-top: 8px; }

      /* 會員視角：純文字為主，不用色塊，數字用粗體與深色字強調 */
      .liff-aud-lead {
        font-size: 14px;
        color: #374151;
        line-height: 1.8;
        margin: 0 0 14px;
      }
      .liff-aud-lead b {
        font-size: 20px;
        font-weight: 700;
        color: #1f2937;
        font-variant-numeric: tabular-nums;
      }
      .liff-aud-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 12px;
      }
      .liff-aud-grid[hidden] { display: none !important; }
      .liff-aud-item {
        padding: 14px 16px;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        background: #fff;
      }
      .liff-aud-k { font-size: 13px; font-weight: 600; color: #1f2937; }
      .liff-aud-v {
        font-size: 26px;
        font-weight: 700;
        color: #1f2937;
        line-height: 1.2;
        margin-top: 6px;
        font-variant-numeric: tabular-nums;
      }
      .liff-aud-sub { font-size: 12px; color: #6b7280; margin-top: 2px; }
      .liff-aud-note { font-size: 11px; color: #9ca3af; margin-top: 10px; line-height: 1.7; }
      .liff-aud-foot { font-size: 13px; color: #6b7280; line-height: 1.8; margin: 14px 0 0; }
      .liff-aud-foot[hidden] { display: none !important; }
      .liff-aud-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 6px 22px;
        margin-top: 14px;
        padding-top: 12px;
        border-top: 1px solid #f3f4f6;
        font-size: 13px;
        color: #4b5563;
      }
      .liff-aud-actions[hidden] { display: none !important; }
      .liff-aud-act b { color: #1f2937; font-weight: 700; font-variant-numeric: tabular-nums; }
      #aud-table-wrap { margin-top: 14px; }
      .liff-aud-since { font-size: 11px; color: #9ca3af; margin-top: 2px; }

      .liff-section-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 8px;
        flex-wrap: wrap;
      }
      .liff-section-head h2 { margin: 0; }
      .liff-range-toggle {
        display: flex;
        gap: 12px;
        font-size: 13px;
        color: #374151;
      }
      .liff-range-toggle label { display: flex; gap: 4px; align-items: center; cursor: pointer; }

      .liff-funnel-wrap { height: 240px; margin-bottom: 12px; }
      .liff-funnel-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 13px;
      }
      .liff-funnel-table th,
      .liff-funnel-table td {
        padding: 8px 10px;
        border-bottom: 1px solid #f3f4f6;
      }
      .liff-funnel-table th { color: #6b7280; font-weight: 600; font-size: 12px; }
      .liff-funnel-table .liff-col-group {
        text-align: center;
        border-bottom: 1px solid #e5e7eb;
        background: #fafafa;
      }
      .liff-funnel-table thead tr:first-child .liff-col-group:nth-child(2) { color: #1d4ed8; }
      .liff-funnel-table thead tr:first-child .liff-col-group:nth-child(3) { color: #b45309; }
      .liff-step-key {
        display: block;
        font-size: 11px;
        color: #9ca3af;
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        margin-top: 2px;
      }

      .liff-trend-wrap { height: 320px; }

      .table-wrap { overflow-x: auto; }
      .liff-users-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 13px;
      }
      .liff-users-table th,
      .liff-users-table td {
        padding: 10px 12px;
        border-bottom: 1px solid #f3f4f6;
        vertical-align: middle;
      }
      .liff-users-table th {
        color: #6b7280;
        font-weight: 600;
        font-size: 12px;
        text-align: left;
        background: #fafafa;
      }
      .liff-user-cell { min-width: 240px; }
      .liff-user-row {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .liff-user-avatar {
        width: 36px;
        height: 36px;
        border-radius: 50%;
        object-fit: cover;
        background: #fde68a;
        color: #1f2937;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 14px;
        flex-shrink: 0;
      }
      .liff-user-avatar--blank { text-transform: uppercase; }
      .liff-user-meta { min-width: 0; }
      .liff-user-name {
        font-weight: 600;
        color: #1f2937;
        display: flex;
        align-items: center;
        gap: 6px;
        flex-wrap: wrap;
      }
      .liff-user-anon { color: #9ca3af; font-style: italic; font-weight: 400; }
      .liff-user-badge {
        font-size: 10px;
        padding: 2px 6px;
        border-radius: 4px;
        font-weight: 600;
        letter-spacing: 0.02em;
      }
      .liff-user-badge--in { background: #d1fae5; color: #065f46; }
      .liff-user-badge--out { background: #f3f4f6; color: #6b7280; }
      .liff-user-uid {
        background: none;
        border: none;
        padding: 2px 0;
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 11px;
        color: #6b7280;
        cursor: pointer;
        margin-top: 2px;
      }
      .liff-user-uid:hover { color: #1f2937; text-decoration: underline; }
      .liff-user-uid.copied { color: #065f46; }
    </style>
    `
  })
%>
